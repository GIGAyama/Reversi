# GIGA Standard v5 監査：リバーシ

実施日：2026-08-04 ／ 型：**B型（Vite + React）** ／ リポジトリ名：`Reversi`

この文書の数字は、**実際に測った値**です。
測っていないものは ✅ にせず「**未計測**」と書いています。

## 測り方

1. `npm run check`（静的な検査・テスト・ビルド）
2. **実ブラウザで開いて測る** … Chromium（Playwright）で
   **4つの画面幅 × 5つの状態 = 20画面**を走査
   - 画面幅：320×568 / 375×667 / 810×1080（iPad）/ 1366×768（Chromebook）
   - 状態：初期画面 / あそびかたモーダル / 1手打った後 / エラー表示中 / 勝敗モーダル
3. **操作して測る** … キーボードだけで石を置く・モーダルを Esc で閉じる・4×4 を終局まで打つ
4. **PWA の挙動を測る** … 登録・更新・オフライン・他アプリのキャッシュ

### 測定環境について（数字の読み方に関わります）

作業環境からは `fonts.googleapis.com` へ出られません。
これは**学校のフィルタリングとまったく同じ状態**なので、
Web フォントが届かない条件で測れています（利点として使いました）。

コントラストは §7-2 の「1px 実際に塗って `getImageData` で読む」方式です。
`oklch()` やグラデーション背景を数字だけで拾うと、
どの要素も「ほぼ真っ黒」と誤判定されて比が 1.0 付近になるためです。

以下は測れていません。

- **`gigayama.github.io` の本番**（プロキシで到達不可）。ローカルの本番ビルドで代用
- **実機**（Chromebook・iPad の現物）。Chromium のエミュレーションで代用
- **iOS Safari 固有の挙動**（ホーム画面追加・ITP）。手順は MANUAL.md に記載のみ
- **実際の教室での 40人同時接続**

---

## 実測（改修前 → 改修後）

| 項目 | 前 | 後 |
|---|---:|---:|
| **コントラスト基準未満** | **15件** | **0件** |
| **タップ44px未満** | **2件** | **0件** |
| CSP違反 | （未導入） | **0件** |
| JSエラー | 0件 | 0件 |
| 320px 幅の横スクロール | 無し | 無し |
| **圏外で起動する** | **いいえ** | **はい** |
| 初回訪問の画面遷移 | 1回 | 1回 |
| 他アプリのキャッシュ巻き添え | 無し | 無し |
| 初回に要る JS | 161.9 KB | **165.3 KB**（gzip 52.5 KB） |
| 総アセット（初回） | 約 219 KB | **227 KB** |
| 中核ロジックのテスト | 0件 | **14件** |
| 品質ゲートの検査 | 0件 | **27件**（全件わざと壊して確認済み） |

### コントラスト基準未満だった 15件の内訳

重いものから。すべて実ブラウザでの実測値です。

| 文言 | 色 | 前 | 後 | どういう文か |
|---|---|---:|---:|---|
| 「開始/リセット」のふりがな | `#8d6e63` | **1.03** | 5.44 | 青ボタンの上のルビ |
| 「もう一度」のふりがな | `#8d6e63` | **1.03** | 5.44 | 勝敗モーダルのボタン |
| 「？」あそびかたボタン | 白 | **1.53** | 6.13 | **入口の案内** |
| 「待った！」のふりがな | `#8d6e63` | **1.68** | 5.16 | オレンジのボタンの上 |
| あそびかた 手順② | 白 | **1.92** | 4.92 | `bg-yellow-500` の既定色 |
| あそびかた 手順① | 白 | **2.28** | 5.09 | `bg-green-500` の既定色 |
| 「待った！」の文字 | 白 | **2.74** | 5.16 | `#ff7043` の上 |
| 「そこには置けません。」 | `#d9534f` | **3.70** | 5.01 | **エラー文** |
| あそびかた 手順③ | 白 | **3.76** | 6.47 | `bg-red-500` の既定色 |
| 「🔊 ON」 | `#0d6efd` | **4.27** | 6.10 | **Bootstrap の既定色そのまま** |
| 「置」等のふりがな（薄地） | `#8d6e63` | **4.31** | 5.01 | ポルカドット背景の上 |

**ふりがなが要るのは低学年の児童**なので、
比 1.03 という状態は「いちばん読めなくて困る人が、いちばん読めない」形でした。

`#0d6efd` は §2-8 の表にある Bootstrap の既定色そのままで、
**アプリ固有の配色ではなくフレームワークの既定色が原因**でした。

#### 測り方で1件見落としかけた

「待った！」は履歴が空のあいだ `disabled` です。
**使用不可の要素は WCAG の対象外**なので測定から除外していましたが、
そのせいで**最初の測定では比 2.74 が見えていませんでした**。
1手打って有効にした状態を追加して測り直し、見つけています。

### タップ44px未満だった 2件

| 対象 | 前 | 後 | 直し方 |
|---|---|---|---|
| 盤面サイズの入力欄 | 48 × **30** | 48 × 44 | `input` は疑似要素を持てないので高さそのものを確保 |
| フッターの「GIGA山」 | **39.2 × 12** | 44 × 44 | 疑似要素で当たり判定だけ広げた（`.tap-44`） |

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama。**改修前は README に記載があるだけで実ファイルが無かった** |
| A2 | .gitignore | ✅ | `node_modules` `dist` に加え `.clasp.json` `.env` を追加 |
| A3 | dependabot.yml | ✅ | monthly。メジャー更新は自動で出さない設定 |
| A4 | README / MANUAL / AUDIT | ✅ | 3点とも作成。README は**実態と食い違っていた**ため全面的に書き直した（下記） |

> **README が実態と食い違っていた。**
> 「HTML/CSS/JSが1ファイルに完結」「`index.html` を編集すればカスタマイズできる」
> 「`favicon.png`」と書かれていたが、実際は Vite + React 構成で
> `index.html` にゲームのコードは1行もない。存在しないファイルの説明もあった。
> 公開URLと著作者名がプレースホルダのままだった。

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（**入れたうえで動作確認済み**） | ✅ | 20画面で**CSP違反 0件・JSエラー 0件**。実際に4×4を終局まで打って確認 |
| B2 | 秘密情報・IDの直書きなし | ✅ | APIキー・シートID・メールアドレスなし。外部通信そのものが無い |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使わない静的アプリ） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage({type:'SKIP_WAITING'})`（Service Worker 宛て。宛先指定不要な形） |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーが無い。個人情報を一切あつかわない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **0 バイト**。React も含め全て自己ホスト。Google Fonts のみ外部だが「見た目だけ」（塞がれた状態で全機能が動くことを実測） |
| B7 | 残る外部資産に SRI と版の固定 | — | 該当なし。外部から読むのは Google Fonts の CSS のみで、`integrity` を付けられない（内容が端末ごとに変わる）。**止まっても動作に影響しない**ことを実測で確認済み |

> **CSP で壊れやすい点への対処。**
> Vite は既定で `modulePreload` のポリフィルを**インラインの `<script>`** として
> 差し込みます。`script-src 'self'` はこれを止めるため、そのままだと起動しません。
> ビルドも静的解析も通るので、**動かさないと気づけない**類の事故でした。
> `vite.config.js` で `modulePreload.polyfill: false` にして外し、
> ビルド後の `index.html` にインラインの `<script>` が1つも無いことを確認しています。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS を使わない） |
| C2 | 自動復旧 | — | 該当なし（永続データを持たない） |
| C3 | pagehide で記録確定 | — | 該当なし。**保存する記録が無い**（対戦状態は端末にも保存していない） |
| C4 | 通信失敗時のリトライと明示 | ✅ | 圏外では `offline.html`。通信そのものはフォント以外に無い |
| C5 | localStorage.clear() を使っていない | ✅ | localStorage を一切使っていない |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | あり |
| D2 | 100dvh を使用 | ✅ | `100dvh` を先に書き `@supports not` でフォールバック |
| D3 | safe-area-inset を適用 | ✅ | 上下左右のパディングに適用。更新の帯も下部セーフエリアを足している |
| D4 | clamp() による fluid type | ✅ | 見出し・スコア・メッセージ・小見出しに適用 |
| D5 | Canvas に devicePixelRatio 補正 | — | 該当なし（**Canvas を使っていない**。盤面は DOM の grid、石は CSS の円） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **20画面すべてで 0件**（`scrollWidth 320 / clientWidth 320`） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は1つも無い。PNG は最大 9.6KB |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **20画面を走査して 0件**（前 15件） |
| D9 | **タップ領域 44px 以上** | ✅ | **20画面を走査して 0件**（前 2件）。疑似要素込みで実測 |
| D10 | prefers-reduced-motion（.01ms） | ✅ | `.01ms`。**0 にすると `fill-mode: forwards` が壊れてモーダルが消える** |
| D11 | forced-colors 対応 | ✅ | ボタン枠・盤面の線・石の輪郭に対応（**改修前は未対応**） |
| D12 | 提示モード | — | **該当なし**。休み時間に個々の端末で遊ぶアプリで、一斉授業では使わない。電子黒板に映す場合はブラウザの全画面＋拡大で足りる（MANUAL.md に記載） |
| D13 | 印刷CSS | — | **該当なし**（印刷する場面が無い。MANUAL.md に明記） |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` なし |

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url が絶対パス | ✅ | 3つとも `/Reversi/`（**改修前は `"./"`**） |
| E2 | アイコン4種 + **透明を含まない** apple-touch-icon | ✅ | 192/512/maskable-192/maskable-512 + apple-touch-icon。**apple-touch-icon の透明画素 0**（180×180、画素を数えて確認）。maskable-192 は今回新規作成 |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `install-hook.js` を `<head>` の最初の `<script>` として同期読み込み |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | `beforeinstallprompt` を受け取れたときだけ「⬇️ アプリにする」を出す |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測**：別アプリ名のキャッシュを2つ置いて版を上げ、**両方とも残った** |
| E6 | sw.js が localStorage に触れていない | ✅ | 触れていない（Service Worker からは触れない） |
| E7 | 更新通知（押すまで切り替わらないことを実測） | ✅ | **実測**：版を上げて3秒放置 → `waiting` のまま・盤面も生きたまま。押した後に切り替わり、旧版キャッシュが消えた |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | **実測：画面遷移 1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | **実測**：`getRegistration()` が `active` を返す |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | CSS も文字も自前、再読み込みは `<a href>`。**サーバーを実際に止めて確認** |
| E11 | APP_VERSION を今回のリリース値に更新 | ✅ | `v3` → **`v4`** |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **192: 0.08% / 512: 0.09%**（画素を数えて測定。下地と中身を色で区別） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | 手順を3ステップで記載 |

> **E10 で実際にバグが1つ見つかりました。**
> 「圏外で起動するか」を測ったところ、**起動しませんでした**。
> 先読み一覧が手書きで、Vite が吐くハッシュ付きの JS / CSS が入っていなかったためです。
> 初回訪問では HTML も JS も Service Worker が有効になる前に読み込まれるので、
> `fetch` ハンドラを通らず runtime キャッシュにも入りません。
> 結果、**本体の HTML は出るのに中身が読み込めず白い画面**になります。
> ビルド時に実ファイル名を埋めるようにして直し、再測定で起動を確認しました。
>
> なお、この測定では最初 `context.setOffline(true)` を使っていましたが、
> **ループバック接続には効かず**、圏外を測れていませんでした。
> 配信サーバー自体を止める方式に変えて測り直しています。

> **`id` の変更について（停止条件の確認）。**
> `id` を `"./"` から `/Reversi/` に変えていますが、
> manifest は `/Reversi/manifest.webmanifest` にあるため
> **`"./"` の解決結果はもともと `/Reversi/` で、同一性は変わりません。**
> すでにインストール済みの端末で別アプリ扱いにはなりません。

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | **実測**：alt の無い `<img>` 0件、aria-label の無いアイコンだけのボタン 0件、`aria-live` 2か所（スコア・メッセージ）、エラーは `role="alert"` |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | **実測**：`role="dialog" aria-modal="true"`、開くと中へ焦点が移り、Esc で閉じ、閉じると元のボタンへ焦点が戻る。Tab はモーダル内で循環 |
| F3 | キーボードのみで全機能に到達 | ✅ | **実測**：Tab で石を置ける場所へ移動し Enter で着手（スコア 2→4 を確認）。全64マスを Tab 対象にすると使えないので、**置ける場所だけ**を対象にしている |
| F4 | rt の色を決め打ちしていない | ✅ | 色のついた面ではまとめて親の色を継がせる（**1か所ずつ潰していない**）。青ボタンの `rt` が白を継いでいることを実測 |
| F5 | 初回JS 300KB以下 | ✅ | **165.3 KB**（gzip 52.5 KB） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `src/App.jsx` の **625行 / 28.4KB** |

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | — | **該当なし**。学習系ではなく休み時間の遊びで、記録を残さない設計。個人情報も一切あつかわない |
| G2 | 中断記録・5分ルール | — | 該当なし（同上） |

---

## 品質ゲート

`npm run check` が CI と同じものを走らせます。
Part I の検査 **27件**を `scripts/lib/giga-v5-checks.mjs` に置いています。

### ゲートを「わざと壊して」確認した

`npm run verify-gate` が、リポジトリを一時ディレクトリへ写して1か所だけ壊し、
その指摘が実際に出るかを 27件分見ます。
**「0件でした」だけでは、検査が動いているのか何も見ていないのか区別できない**ためです。

**この確認によって、検査そのものの不具合が 2件見つかりました。**

| 見つかった不具合 | 中身 |
|---|---|
| `SAFE_AREA` の取りこぼし | 「リポジトリのどこかに `safe-area-inset` があるか」で判定していたため、**アプリ本体の CSS からまるごと消しても `offline.html` に同じ語があるせいで指摘が出なかった**。アプリ本体のスタイルだけを見るよう直した |
| `FORCED_COLORS` の取りこぼし | 同上 |

同じ理由で `RT_HARDCODED` も「ファイルごとに見る」形へ直しています。

### 手元では通るのに CI だけ落ちた件

テストの実行を `node --test "tests/**/*.test.js"` と書いていました。
これは **Node 自身にグロブを展開させる書き方で、Node 22 以降でしか動きません。**
手元が Node 22、CI のランナーが Node 20 だったため、
**手元では 14件通るのに CI だけが `Could not find 'tests/**/*.test.js'` で落ちました。**

`scripts/run-tests.mjs` がファイルを自分で数え上げて渡す形に変え、
版にもシェルにも依存しないようにしています。
**Node 20.20.2（CI と同じ版）と Node 22.22.2 の両方で `npm run check` と
`npm run verify-gate` が通ることを確認済みです。**
あわせて、GitHub が非推奨にした Node 20 からランナーを 22 に上げました。

「手元で通ったから大丈夫」は、実行環境が違えば成り立ちません。

### 正本について（未取得）

§P4 は `scripts/lib/project-quality.mjs`（フリート共通の検査の正本）を
**バイト単位でコピー**して合成する構成を求めていますが、
**この正本はまだこのリポジトリに取り込めていません**（別リポジトリにあり、今回の作業範囲外）。

`scripts/check-project.mjs` は正本があれば読み込んで合成する形にしてあり、
無い場合は毎回 `CANONICAL_MISSING` として**未取得であることを出力します**。
黙って「0件でした」と通さないためです。

---

## 残っている課題・人間に決めてほしいこと

1. **`npm audit` の残り2件**
   `esbuild`（moderate）と `vite`（high）。**修正には vite のメジャー更新（5→8）が要る**ため、
   当てていません（§改修モード規則6）。
   いずれも**開発サーバーの脆弱性で、ブラウザへ配る成果物には入りません**。
   上げるかどうかは人の判断です。

2. **`scripts/lib/project-quality.mjs`（正本）の取り込み**
   どのリポジトリを正本とするか決まり次第、バイト単位でコピーしてください。

3. **本番での確認**
   `gigayama.github.io` へ到達できないため、**公開後の実機確認は行えていません。**
   マージ後に次を確認してください。
   - ホーム画面のアイコンが正しく出るか（特に iPad で四隅が黒くならないか）
   - すでにインストール済みの端末で、別アプリ扱いになっていないか
   - 「アプリにする」ボタンが出るか

4. **ファイル分割**
   中核ロジックを `src/lib/reversi.js` へ分けました（テストを書けるようにするため）。
   これはアーキテクチャの変更にあたるので、**マージの可否は人の判断**に委ねます。
